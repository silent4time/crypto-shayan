# Changelog

تمام تغییرات قابل توجه این فورک (نسخه‌ی سخت‌شده‌ی امنیتی افزونه‌ی CryptoPay WooCommerce) در این فایل ثبت می‌شود.

قالب نسخه‌گذاری: `<نسخه‌ی اصلی از BeycanPress>-security.<شماره پچ این فورک>`
یعنی هر بار که این مخزن به‌روزرسانی می‌شود، فقط بخش `security.N` افزایش پیدا می‌کند؛ این پروژه یک فورک مستقل و بدون ارتباط با آپدیت رسمی ناشر اصلیه.

---

## [2.4.5-security.1] - 2026-08-29

اولین نسخه‌ی این فورک، بر پایه‌ی نسخه‌ی رسمی **2.4.5** افزونه‌ی CryptoPay WooCommerce (BeycanPress). این نسخه چند مشکل امنیتی شناسایی‌شده در بازبینی کد را رفع می‌کند و یک قابلیت جدید مستقل اضافه می‌کند.

### 🔒 امنیت (Fixed)

- **بستن دسترسی عمومی به REST API** — تمام endpointهای پرداخت (`check-payment`, `save-transaction`, `payment-finished`, `currency-converter`) پیش‌تر بدون هیچ بررسی مالکیت سفارش، برای هر بازدیدکننده‌ی ناشناسی در دسترس بودند. حالا:
  - برای سفارش‌های کاربر لاگین‌شده، شناسه‌ی کاربر فعلی باید با صاحب سفارش یکی باشد.
  - برای سفارش‌های مهمان، شناسه‌ی سفارش باید با مقدار `order_awaiting_payment` در سشن ووکامرس همان بازدیدکننده مطابقت داشته باشد.
  - فیلتر جدید `CryptoPay/WooCommerce/OwnsOrder` برای سفارشی‌سازی این رفتار در صورت نیاز اضافه شد.
  - فایل‌ها: `app/PluginHero/Helpers.php`, `app/Payment/CheckoutApi.php`

- **حذف `serialize()`/`unserialize()`** — اطلاعات تراکنش اکنون با `wp_json_encode()`/`json_decode()` ذخیره و خوانده می‌شود تا ریسک PHP Object Injection از بین برود. برای سازگاری با داده‌های قدیمی، در صورت نبود JSON معتبر یک fallback امن (`allowed_classes: false`) اجرا می‌شود.
  - فایل‌ها: `app/PluginHero/Helpers.php`, `app/Payment/CheckoutApi.php`, `app/Services/Verifier.php`, `app/Payment/Details.php`

- **اعتبارسنجی دقیق‌تر آدرس کیف‌پول** — بررسی صرفِ طول رشته (۴۲ کاراکتر) با یک regex واقعی جایگزین شد که فرمت `0x` + ۴۰ کاراکتر هگزادسیمال را چک می‌کند.
  - فایل‌ها: `app/PluginHero/Helpers.php`, `app/Settings.php`

### ✨ ویژگی جدید (Added)

- **محدودسازی نرخ درخواست (Rate limiting)** — یک throttle ساده بر اساس IP به تمام endpointهای عمومی افزونه اضافه شد (پیش‌فرض: حداکثر ۳۰ درخواست در هر ۶۰ ثانیه برای هر IP). قابل تنظیم با فیلترهای `CryptoPay/WooCommerce/RateLimit` و `CryptoPay/WooCommerce/RateLimitWindow`.
  - فایل: `app/Payment/CheckoutApi.php`

### ⚠️ نکات ارتقا

- بخش تشخیص سفارش «مهمان» به رفتار داخلی ووکامرس (`WC()->session`) وابسته است. قبل از استفاده در سایت اصلی، حتماً یک خرید تستی (هم با کاربر لاگین‌شده و هم مهمان) روی محیط staging انجام دهید.
- این نسخه یک فورک مستقل غیررسمی است و هیچ ارتباطی با کانال آپدیت رسمی BeycanPress ندارد؛ آپدیت‌های امنیتی/فیچر آینده‌ی این مخزن هم زیر همین `CHANGELOG.md` ثبت می‌شوند.
